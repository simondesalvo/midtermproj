using System;
using System.Collections.Generic;
using System.Text;

namespace midtermproj
{
    class FormerMember
    {
        //Add memebers somehow and get them on the I/O
        
        
        //Calls CheckIn method (chekcs a particular member in at a particular club
        //CheckIn(){ (number code (100) + [i]+ member[i].name) }   needs to have an exception error so program doesn't crash
        
        //Select a member and generate a bill of fees that include membership points for multi-club members
        //cw(display list)
        //cw (which member do you want to select?)
        //displays fees that they owe and how many points they have



    }

    
}
